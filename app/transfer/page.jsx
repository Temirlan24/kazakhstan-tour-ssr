import TransferClient from '@/components/TransferClient';

export const metadata = {
  title: 'Airport Transfer in Almaty — Crown Almaty Services',
  description: 'Reliable airport and city transfers in Almaty. Economy, Business, Minivan and VIP options. Book via WhatsApp.',
};

export default function TransferPage() {
  return <TransferClient />;
}
